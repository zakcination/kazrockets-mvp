# Database Configuration
DATABASE_URL=postgresql+asyncpg://kazrockets:password@localhost:5432/kazrockets
TEST_DATABASE_URL=postgresql+asyncpg://kazrockets:password@localhost:5432/kazrockets_test

# Redis Configuration
REDIS_URL=redis://localhost:6379

# RabbitMQ Configuration
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# MinIO Configuration (S3 Mock)
MINIO_URL=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=kazrockets-submissions

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Application Configuration
API_V1_STR=/api/v1
PROJECT_NAME=KazRockets MVP
VERSION=1.0.0
DESCRIPTION=Robotics Competition Management Platform
BACKEND_CORS_ORIGINS=["http://localhost:3000", "https://localhost:3000"]

# Environment
ENVIRONMENT=development

# Email Configuration (optional for MVP)
SMTP_TLS=True
SMTP_PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-password
EMAILS_FROM_EMAIL=noreply@kazrockets.com
EMAILS_FROM_NAME=KazRockets Platform

# File Upload Configuration
MAX_FILE_SIZE_MB=10
ALLOWED_FILE_TYPES=["application/pdf"]

# Security
BCRYPT_ROUNDS=12

# Frontend Configuration
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# Docker Configuration
POSTGRES_USER=kazrockets
POSTGRES_PASSWORD=password
POSTGRES_DB=kazrockets

# Testing
PYTEST_XDIST_WORKER_COUNT=auto